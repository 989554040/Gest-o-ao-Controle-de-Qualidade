name: Pipeline de Qualidade

on:
  push:
    branches: [ "main" ]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Teste automatizado básico
        run: echo "Teste automatizado executado com sucesso"
